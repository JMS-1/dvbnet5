{
    "configurations": [
        {
            "name": "Debug",
            "preLaunchTask": "watch",
            "request": "launch",
            "sourceMapPathOverrides": {
                "webpack://vcrnet-web-client/*": "${workspaceFolder}/*"
            },
            "sourceMaps": true,
            "type": "chrome",
            "url": "http://localhost:3000",
            "userDataDir": "${workspaceFolder}/.vscode/chrome",
            "webRoot": "${workspaceFolder}"
        }
    ],
    "version": "0.2.0"
}
